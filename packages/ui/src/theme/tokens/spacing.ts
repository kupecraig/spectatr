import type { ThemeOptions } from '@mui/material/styles';

/**
 * Spacing configuration
 * Default: 8px base unit (theme.spacing(1) = 8px)
 */
export const spacingConfig: ThemeOptions['spacing'] = 8;
